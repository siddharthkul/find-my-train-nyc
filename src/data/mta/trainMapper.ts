/**
 * @deprecated  Use `mappers/vehicleMapper.ts` instead.
 *
 * Backward-compatible re-export for existing imports.
 */
export { mapVehiclePositions as mapFeedToTrains } from './mappers/vehicleMapper';
